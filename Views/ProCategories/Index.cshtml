@model IEnumerable<yektaApi.Models.ProCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CPName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CPInfo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CPName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CPInfo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CPId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CPId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CPId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
